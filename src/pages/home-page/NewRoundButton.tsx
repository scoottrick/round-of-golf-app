import React from 'react';
import { RectButton } from '../../components';

const NewRoundButton = ({ onClick }) => {
  return <RectButton onClick={onClick}>Start a Round</RectButton>;
};

export default NewRoundButton;
